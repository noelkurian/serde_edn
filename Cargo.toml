[package]
name = "serde_edn"
version = "0.1.0"
edition = "2021"
description = "EDN serialization/deserialization for Rust"
authors = ["You"]
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = "0.4"
uuid = "1.0"

[dev-dependencies]
